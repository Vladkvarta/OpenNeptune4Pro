#############################################################################
#   Print Start & End Macros
#############################################################################
[gcode_macro PRINT_START]
variable_small_print: False
variable_outer_bed_force_activation_temp: 80
variable_force_outer_bed_heater: False
gcode:
    {% set BED_TEMP = params.BED_TEMP|default(60)|float %}
    {% set EXTRUDER_TEMP = params.EXTRUDER_TEMP|default(200)|float %}
    {% set BED_MESH = params.BED_MESH|default('adaptive')|string %}

    {% if BED_TEMP >= outer_bed_force_activation_temp %}
      SET_GCODE_VARIABLE MACRO=PRINT_START VARIABLE=force_outer_bed_heater VALUE=True
    {% else %}
      SET_GCODE_VARIABLE MACRO=PRINT_START VARIABLE=force_outer_bed_heater VALUE=False
    {% endif %}

    ; --- Фаза 1: Свет, парковка и преднагрев ---
    Frame_Light_ON
    Part_Light_ON
    G92 E0
    G90
    BED_MESH_CLEAR
    SET_HEATER_TEMPERATURE HEATER=extruder TARGET=150
    M140 S{BED_TEMP}
    CG28
    ; Eddy probe in center of bed: nozzle at (140.5, 94.5) → probe at (117.5, 117.5)
    G1 X140.5 Y94.5 Z1 F6000
    M117 Heating...

    ; --- Нагрев стола и стабилизация Eddy ---
    M190 S{BED_TEMP}
    RESPOND MSG="Warming up Eddy probe for 60 seconds..."
    G4 P60000
    TEMPERATURE_WAIT SENSOR=extruder MINIMUM=145 MAXIMUM=155

    ; --- Фаза 2: Z-Homing и сетка ---
    RESPOND MSG="Z-Homing with warm Eddy..."
    G28 Z
    {% if BED_MESH != 'none' %}
      BED_MESH_PROFILE LOAD=default
      RESPOND MSG="Static BED_MESH 'default' loaded. Skipping dynamic scan."
    {% endif %}
    SET_FILAMENT_SENSOR SENSOR=filament_sensor ENABLE=1

    ; --- Финальный нагрев и очистка ---
    M109 S{EXTRUDER_TEMP}
    LINE_PURGE
    G92 E0
    G1 Z2.0 F3000
    M117 Printing...

; Алиасы для совместимости со старыми gcode файлами слайсера
[gcode_macro PRINT_START_1]
gcode:
    RESPOND MSG="PRINT_START_1 alias called - use PRINT_START instead"

[gcode_macro PRINT_START_2]
gcode:
    RESPOND MSG="PRINT_START_2 alias called - use PRINT_START instead"

[gcode_macro PRINT_START_3]
gcode:
    RESPOND MSG="PRINT_START_3 alias called - use PRINT_START instead"

[gcode_macro PRINT_START_1_PREHEAT]
description: Phase 1 - Парковка и подготовка к прогреву
gcode:
    Frame_Light_ON
    Part_Light_ON
    G92 E0 
    G90
    BED_MESH_CLEAR    

    {% set BED_TEMP = params.BED_TEMP|default(60)|float %}
    {% set EXTRUDER_TEMP_PREHEAT = 150 %}
    # Запускаем набор температур (без ожидания)
    SET_HEATER_TEMPERATURE HEATER=extruder TARGET={EXTRUDER_TEMP_PREHEAT}
    M140 S{BED_TEMP} 
    
    CG28 ; Безопасная предварительная парковка
    # Едем в центр стола, чтобы датчик Eddy грелся от его центра
    ; Eddy probe in center of bed: nozzle at (140.5, 94.5) → probe at (117.5, 117.5)
    G1 X140.5 Y94.5 Z1 F6000
    M117 Heating Phase...
    # Дальше слайсер должен сам вызвать M190, G4 и M109!

[gcode_macro PRINT_START_2_PROBE]
description: Phase 2 - Точный хоуминг, Сетка и Парковка
gcode:
    {% set BED_MESH = params.BED_MESH|default('adaptive')|string %}
    
    # ПРИНУДИТЕЛЬНАЯ точная парковка Z на рабочих температурах стола и прогретом датчике
    RESPOND MSG="Z-Homing with warm Eddy..."
    G28 Z 
    
    {% if BED_MESH != 'none' %}
      BED_MESH_PROFILE LOAD=default
      RESPOND MSG="Static BED_MESH 'default' loaded. Skipping dynamic scan."
    {% endif %}

    SET_FILAMENT_SENSOR SENSOR=filament_sensor ENABLE=1
    M117 Final Heating...
    # Дальше слайсер вызывает M109 для финальной температуры!

[gcode_macro PRINT_START_3_PURGE]
description: Phase 3 - Очистка и запуск
gcode:
    LINE_PURGE
    G92 E0
    G1 Z2.0 F3000
    M117 Printing...
       
[gcode_macro PRINT_END]
gcode:
    Frame_Light_OFF
    Part_Light_OFF
    M400
    TURN_OFF_HEATERS
    
    ;# --- 1. Сброс давления и отскок ---
    G92 E0
    G91 ; Относительные координаты
    G1 E-2 F2700 ; Быстрый первичный откат
    G1 X5 Y5 F3000 ; Смещение в сторону от детали (Wipe)
    G1 E-2 F1600 ; Дополнительный откат для чистоты сопла
    
    ;# --- 2. УМНЫЙ ПОДЪЕМ НА 50 ММ ---
    {% set max_z = printer.toolhead.axis_maximum.z|float %}
    {% set act_z = printer.toolhead.position.z|float %}
    
    ;# Если до потолка больше 50 мм - поднимаем на 50. Иначе - до упора.
    {% if act_z < (max_z - 50.0) %}
        {% set z_safe = 50.0 %}
    {% else %}
        {% set z_safe = max_z - act_z %}
    {% endif %}
    
    G1 Z{z_safe} F1500 ; Сам подъем
    
    ;# --- 3. Парковка и выключение ---
    G90 ; Возврат в абсолютные координаты
    G1 X0 Y230 F6000 ; Голову влево, стол выкатываем вперед
    
    M107 ; Выключение обдува детали
    
    ;# ВАЖНО: Отключаем только моторы X, Y и Экструдера. Ось Z оставляем включенной!
    M84 X Y E 
    
    SET_FILAMENT_SENSOR SENSOR=filament_sensor ENABLE=0
    M117
    END_TUNE

#############################################################################
#   Pause, Resume & Cancel Macros
#############################################################################
[gcode_macro PAUSE]
rename_existing: BASE_PAUSE
gcode:
  PAUSE_TUNE
  {% set z = params.Z|default(30)|int %}
  {% if printer['pause_resume'].is_paused|int == 0 %}
      SET_GCODE_VARIABLE MACRO=RESUME VARIABLE=etemp VALUE={printer['extruder'].target|default(220)}
      SET_GCODE_VARIABLE MACRO=LOAD_FILAMENT VARIABLE=prev_temp VALUE={printer['extruder'].target|default(220)}
      SET_GCODE_VARIABLE MACRO=UNLOAD_FILAMENT VARIABLE=prev_temp VALUE={printer['extruder'].target|default(220)}
      SET_GCODE_VARIABLE MACRO=RESUME VARIABLE=zhop VALUE={z}
      SET_FILAMENT_SENSOR SENSOR=filament_sensor ENABLE=0
      SAVE_GCODE_STATE NAME=PAUSE
      BASE_PAUSE
      G91
      {% if (printer.gcode_move.position.z + z) < printer.toolhead.axis_maximum.z %}
          G1 E-2 F2700
          G1 X3 Y3 F3000
          G1 E-2 Z{z} F1600
          SAVE_GCODE_STATE NAME=ZLIFT
      {% else %}
          { action_respond_info("Pause zhop exceeds maximum Z height.") }
          SET_GCODE_VARIABLE MACRO=RESUME VARIABLE=zhop VALUE=0
          G1 E-2 F2700
          G1 X3 Y3 F3000
          G1 E-2 F1600
          SAVE_GCODE_STATE NAME=ZLIFT
      {% endif %}
      G90
      G1 X{printer.toolhead.axis_minimum.x+5} Y{printer.toolhead.axis_maximum.y-5} F6000
      SAVE_GCODE_STATE NAME=PAUSEPARK
      SET_HEATER_TEMPERATURE HEATER=extruder TARGET=175
      SET_IDLE_TIMEOUT TIMEOUT=43200
  {% endif %}

[gcode_macro RESUME]
rename_existing: BASE_RESUME
variable_zhop: 0
variable_etemp: 0
gcode:
  {% if printer['pause_resume'].is_paused|int == 1 %}
      SET_FILAMENT_SENSOR SENSOR=filament_sensor ENABLE=1
      SET_IDLE_TIMEOUT TIMEOUT={printer.configfile.settings.idle_timeout.timeout}
      {% if etemp > 0 %}
          SET_HEATER_TEMPERATURE HEATER=extruder TARGET={etemp|int}
          TEMPERATURE_WAIT SENSOR=extruder MINIMUM={etemp|int - 4} MAXIMUM={etemp|int + 10}
      {% endif %}
      RESTORE_GCODE_STATE NAME=PAUSEPARK MOVE=1 MOVE_SPEED=100
      G91
      M83
      G1 E80 F200
      G4 P2000
      G1 X20 F15000
      G1 X-20
      G1 X20
      G1 X-20
      G1 X20
      G1 X-20
      RESTORE_GCODE_STATE NAME=ZLIFT MOVE=1 MOVE_SPEED=60
      G1 X-3 Y-3 F3000
      G1 Z{zhop * -1} F900  
      RESTORE_GCODE_STATE NAME=PAUSE MOVE=1 MOVE_SPEED=60
      M400
      BASE_RESUME
  {% endif %}

[gcode_macro CANCEL_PRINT]
rename_existing: BASE_CANCEL_PRINT
gcode:
  # 1. Возвращаем таймауты
  SET_IDLE_TIMEOUT TIMEOUT={printer.configfile.settings.idle_timeout.timeout}
  # 2. Сброс состояния чтения и очистка очереди
  CLEAR_PAUSE
  SDCARD_RESET_FILE
  BASE_CANCEL_PRINT
  
  # 3. Выключение всех нагревов СРАЗУ
  TURN_OFF_HEATERS
  
  # 4. Безопасный подъём и отвод сопла, без глупых M400 и ретрактов "на холодную"!
  {% if printer.toolhead.homed_axes == "xyz" %}
      G91
      # Поднимаем на 10 мм
      G1 Z10 F1500
      G90
      # База вперёд, голова влево
      G1 X0 Y230 F6000
  {% endif %}
  
  # 5. Гасим всё остальное
  M107 ; Выключение обдува детали
  M84 X Y E 
  Frame_Light_OFF
  Part_Light_OFF
  SET_FILAMENT_SENSOR SENSOR=filament_sensor ENABLE=0
  M117 Отмена печати!
  RESPOND MSG="Печать мгновенно прервана. Нагрев отключен."

